# ============================================
# 环境配置示例文件
# ============================================
# 使用说明:
# 1. 复制此文件为 .env (团队共享配置)
# 2. 复制此文件为 .env.local (个人配置,不会提交到Git)
# 3. .env.local 中的配置会覆盖 .env 中的同名配置

# 应用配置
FLASK_ENV=development
SECRET_KEY=your-secret-key-here-change-this-in-production

# 数据库配置
# 默认使用代码中的自动路径,如需自定义请取消注释并修改
# DATABASE_URL=sqlite:///app.db
# DEV_DATABASE_URL=sqlite:///app-dev.db

# 使用 PostgreSQL 示例:
# DEV_DATABASE_URL=postgresql://username:password@localhost:5432/eduinsight_dev

# 使用 MySQL 示例:
# DEV_DATABASE_URL=mysql://username:password@localhost:3306/eduinsight_dev

# 使用自定义路径的 SQLite 示例:
# DEV_DATABASE_URL=sqlite:///D:\my-projects\my-custom-db.db

# 服务器配置
PORT=5030
DEBUG=True

# CORS配置
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174
